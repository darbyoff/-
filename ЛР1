#include <iostream>

// Функция для возведения числа base в степень power
double power(double base, int power) {
    double result = 1.0;
    for (int i = 0; i < power; i++) {
        result *= base;
    }
    return result;
}

int main() {
    double base;
    int exponent;

    std::cout << "Введите основание: ";
    std::cin >> base;

    std::cout << "Введите степень: ";
    std::cin >> exponent;

    double result = power(base, exponent);
    std::cout << base << " в степени " << exponent << " = " << result << std::endl;

    return 0;
}

Теперь разберем основные части этой программы:

#include <iostream>: Эта строка включает заголовочный файл iostream, который содержит определения для стандартных потоков ввода/вывода (например, cin и cout).
double power(double base, int power): Здесь объявлена функция power, которая принимает два аргумента: основание (base) типа double и степень (power) типа int. Функция возвращает результат типа double.
int main(): Это главная функция программы, с которой начинается её выполнение.
std::cout, std::cin: Эти объекты предоставляют возможность вывода данных в консоль (cout) и считывания данных с консоли (cin).
return 0;: Эта инструкция завершает функцию main и сообщает операционной системе об успешном завершении программы.
Теперь касательно модификации программы для вычисления степени с любым основанием: в данном случае, достаточно изменить тип переменной base на double, чтобы программа могла работать с числами с плавающей запятой.

Надеюсь, эта информация поможет вам лучше понять структуру программы на языке C++. Если у вас есть конкретные вопросы или нужна дополнительная помощь, не стесняйтесь спрашивать.
